import React from "react";

const Heading = ({ title }) => {
  return (
    <div className="mb-6 font-dm text-xl font-bold md:mb-12 md:text-[39px]">
      {title}
    </div>
  );
};

export default Heading;
